module:
  hugoVersion:
    extended: true
    min: 0.104.0

outputFormats:
  Schematize:
    mediaType: application/json
    baseName: schema
    isPlainText: true

params:
  Platen:
    Theme:
      Shortcodes:
        Schematize:
          Enabled: true
          Style:
            Name: schematize
    Markup:
      Schematize:
        Enabled: true
        Partials:
          Renderers:
            Link: schematize/sref/markup/renderers/links
