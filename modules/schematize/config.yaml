module:
  hugoVersion:
    extended: true
    min: 0.108.0

outputFormats:
  schematize:
    mediaType: application/json
    baseName: schema
    isPlainText: true

params:
  platen:
    shortcodes:
      schematize:
        enabled: true
        style:
          name: schematize
    markup:
      schematize:
        enabled: true
        partials:
          renderers:
            link: schematize/sref/markup/renderers/links
  schematize:
    schema_root: schemas