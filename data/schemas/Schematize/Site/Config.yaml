# yaml-language-server: $schema=https://json-schema.org/draft/2020-12/schema
$schema: https://json-schema.org/draft/2020-12/schema
$id: /schemas/Schematize/Site/Config.schema.yaml
title: Site Configuration
description: Settings and options for the Schematize module at the site level.
schematize:
  details: |
    The Schematize module makes it easier to author and publish JSON schemas with documentation
    from YAML files. These settings effect the way the schemas are munged and how the documentation
    is rendered.
type: object
properties:
  ReferenceMap:
    $ref: ./ReferenceMap.yaml